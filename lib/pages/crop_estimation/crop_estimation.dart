import 'package:flutter/material.dart';

class CropEstimation extends StatelessWidget {
  const CropEstimation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(child: Text('Crop Estimation'),)
    );
  }
}
